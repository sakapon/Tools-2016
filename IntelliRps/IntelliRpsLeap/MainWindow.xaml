<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IntelliRpsLeap"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity" xmlns:ei="http://schemas.microsoft.com/expression/2010/interactions"
        x:Class="IntelliRpsLeap.MainWindow"
        mc:Ignorable="d"
        Title="Intelli-RPS" Height="800" Width="1200" WindowState="Maximized" FontSize="32">
    <Window.Resources>
        <local:FuncConverter x:Key="ToImagePathConverter" ToFunc="{x:Static local:MainViewModel.ToImagePath}"/>
    </Window.Resources>
    <Window.DataContext>
        <local:MainViewModel/>
    </Window.DataContext>
    <Viewbox>
        <Grid Width="1800" Height="1200" Margin="30">
            <Image Source="{Binding AppModel.HandTracker.HandShape.Value, Converter={StaticResource ToImagePathConverter}, Mode=OneWay}" HorizontalAlignment="Right" VerticalAlignment="Bottom" Width="160" Height="160"/>
        </Grid>
    </Viewbox>
</Window>
